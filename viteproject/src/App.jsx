import { useState } from 'react'

function App() {
  const [count, setCount] = useState(0)

  return (
    <h1>Hello world | Fahad Bhatti 
      firstly we install git and make it comfortable with github 
      then we open react documentation
      then we create react app
      and instal some pkgs though npm 
      and then make changes in the src files
      and remove the unusable files 
      so thats our first project

    </h1>
  )
}

export default App
